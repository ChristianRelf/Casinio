import { notFound } from "next/navigation";import { CardBack, PlayingCard, RANKS, SUITS } from "../../../components/cards/PlayingCard";
export default function DeckGallery(){if(process.env.NODE_ENV==="production")notFound();return <main className="deck-gallery"><header><span className="overline">INTERNAL ASSET QA</span><h1>Low Stakes — complete deck</h1><p>52 faces and one back. Check corner indices, pip balance, court consistency, contrast, and mobile readability.</p></header><section>{SUITS.map(suit=><div className="suit-row" key={suit}><h2>{suit}</h2><div>{RANKS.map(rank=><PlayingCard rank={rank} suit={suit} key={`${rank}-${suit}`}/>)}</div></div>)}</section><aside><h2>Back</h2><CardBack/></aside></main>}
