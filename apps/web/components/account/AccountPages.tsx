"use client";
import { FormEvent, useEffect, useState } from "react";import { useRouter } from "next/navigation";import type { SessionUser } from "../../packages/contracts/src";import { api, post } from "../../lib/client/api";import { CasinoNav } from "../ui/CasinoNav";

type Overview={user:SessionUser;wallet:{balance:number;last_refill_at:string|null};stats:{rounds_played:number;hands_won:number;hands_lost:number;hands_pushed:number;blackjacks:number;biggest_win:number;total_wagered:number}};
function useAccount(){const router=useRouter();const[data,setData]=useState<Overview|null>(null);const[error,setError]=useState("");const load=()=>api<Overview>("me").then(setData).catch(reason=>{if((reason as {status?:number}).status===401)router.replace("/");else setError(reason instanceof Error?reason.message:"Account could not be loaded")});useEffect(()=>{void load()},[]);return{data,error,load}}
function PageFrame({user,title,kicker,children}:{user:SessionUser;title:string;kicker:string;children:React.ReactNode}){return <main className="support-screen"><CasinoNav user={user}/><div className="support-wrap"><header className="support-heading"><span className="overline">{kicker}</span><h1>{title}</h1></header>{children}</div></main>}
export function ProfilePage(){const{data,error,load}=useAccount();if(!data)return <main className="loading-screen"><p>{error||"Loading profile…"}</p></main>;const s=data.stats;const played=s.rounds_played||0;return <PageFrame user={{...data.user,balance:data.wallet.balance}} kicker="PLAYER PROFILE" title={data.user.displayName}><section className="profile-grid"><article className="profile-balance"><span>PLAY-MONEY BALANCE</span><b>${data.wallet.balance.toLocaleString()}</b><p>No cash value. No purchase history because there are no purchases.</p><button className="outline-button" onClick={async()=>{try{await post("me/refill",{});await load()}catch{}}}>Claim daily recovery allowance</button></article><div className="stat-grid"><article><span>ROUNDS</span><b>{played}</b></article><article><span>HANDS WON</span><b>{s.hands_won}</b></article><article><span>BLACKJACKS</span><b>{s.blackjacks}</b></article><article><span>BIGGEST WIN</span><b>${s.biggest_win.toLocaleString()}</b></article><article><span>WIN RATE</span><b>{s.hands_won+s.hands_lost?Math.round(s.hands_won/(s.hands_won+s.hands_lost)*100):0}%</b></article><article><span>PUSHES</span><b>{s.hands_pushed}</b></article></div></section></PageFrame>}
export function HistoryPage(){const{data,error}=useAccount();const[ledger,setLedger]=useState<Array<Record<string,string|number|null>>>([]);useEffect(()=>{api<Array<Record<string,string|number|null>>>("me/ledger").then(setLedger).catch(()=>undefined)},[]);if(!data)return <main className="loading-screen"><p>{error||"Loading history…"}</p></main>;return <PageFrame user={{...data.user,balance:data.wallet.balance}} kicker="SESSION HISTORY" title="Every chip accounted for."><section className="ledger-card"><header><span>RECENT BALANCE EVENTS</span><b>{ledger.length} entries</b></header>{ledger.length?ledger.map(item=><div className="ledger-row" key={String(item.id)}><span className={`ledger-amount ${Number(item.amount)>0?"positive":Number(item.amount)<0?"negative":""}`}>{Number(item.amount)>0?"+":""}${Number(item.amount).toLocaleString()}</span><div><b>{String(item.reason).replaceAll("_"," ")}</b><small>{new Date(String(item.created_at)).toLocaleString()}</small></div><span>${Number(item.balance_after).toLocaleString()}</span></div>):<div className="history-empty">No rounds yet. The ledger is enjoying the quiet.</div>}</section></PageFrame>}
export function SettingsPage(){const{data,error}=useAccount();const[prefs,setPrefs]=useState({sound:false,effectsVolume:.65,notificationVolume:.6,shake:true,reducedMotion:false});useEffect(()=>{try{const value=localStorage.getItem("ls_preferences");if(value)setPrefs(current=>({...current,...JSON.parse(value)}))}catch{}},[]);const update=(next:typeof prefs)=>{setPrefs(next);localStorage.setItem("ls_preferences",JSON.stringify(next))};if(!data)return <main className="loading-screen"><p>{error||"Loading settings…"}</p></main>;return <PageFrame user={{...data.user,balance:data.wallet.balance}} kicker="ACCESSIBILITY & SOUND" title="Make the table behave."><section className="settings-card"><div className="setting-row"><div><b>Sound</b><p>Procedural local effects. Muted until you enable them.</p></div><button className={`switch ${prefs.sound?"on":""}`} onClick={()=>update({...prefs,sound:!prefs.sound})} aria-pressed={prefs.sound}><i/></button></div><div className="setting-row range-row"><div><b>Game effects volume</b><p>Cards, chips, results, and buttons.</p></div><input aria-label="Game effects volume" type="range" min="0" max="1" step=".05" value={prefs.effectsVolume} onChange={event=>update({...prefs,effectsVolume:Number(event.target.value)})}/></div><div className="setting-row range-row"><div><b>Notification volume</b><p>Turn changes and table notices.</p></div><input aria-label="Notification volume" type="range" min="0" max="1" step=".05" value={prefs.notificationVolume} onChange={event=>update({...prefs,notificationVolume:Number(event.target.value)})}/></div><div className="setting-row"><div><b>Impact shake</b><p>Affects only the table presentation and scales with a result.</p></div><button className={`switch ${prefs.shake?"on":""}`} onClick={()=>update({...prefs,shake:!prefs.shake})} aria-pressed={prefs.shake}><i/></button></div><div className="setting-row"><div><b>Reduce motion</b><p>Disables dealing travel, celebrations, and table shake.</p></div><button className={`switch ${prefs.reducedMotion?"on":""}`} onClick={()=>update({...prefs,reducedMotion:!prefs.reducedMotion})} aria-pressed={prefs.reducedMotion}><i/></button></div></section></PageFrame>}
export function AdminPage(){const{data,error}=useAccount();const[users,setUsers]=useState<Array<Record<string,string|number>>>([]);const[message,setMessage]=useState("");useEffect(()=>{api<Array<Record<string,string|number>>>("admin/users").then(setUsers).catch(reason=>setMessage(reason instanceof Error?reason.message:"Admin access unavailable"))},[]);async function adjust(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);try{await post(`admin/users/${form.get("userId")}/wallet`,{operation:form.get("operation"),amount:Number(form.get("amount")),reason:form.get("reason")});setMessage("Adjustment committed and audited.");setUsers(await api<Array<Record<string,string|number>>>("admin/users"))}catch(reason){setMessage(reason instanceof Error?reason.message:"Adjustment failed")}}if(!data)return <main className="loading-screen"><p>{error||"Loading administration…"}</p></main>;return <PageFrame user={{...data.user,balance:data.wallet.balance}} kicker="ROLE-PROTECTED" title="Casino administration"><section className="admin-layout"><form className="admin-form" onSubmit={adjust}><h2>Adjust play money</h2><label>Player<select name="userId">{users.map(user=><option value={String(user.id)} key={String(user.id)}>{String(user.display_name)} — ${Number(user.balance).toLocaleString()}</option>)}</select></label><label>Operation<select name="operation"><option value="grant">Grant</option><option value="remove">Remove</option><option value="reset">Reset to starting balance</option></select></label><label>Amount<input name="amount" type="number" min="0" defaultValue="500"/></label><label>Audit reason<input name="reason" required minLength={3} maxLength={200}/></label><button className="cream-button">Commit adjustment</button>{message&&<p role="status">{message}</p>}</form><div className="admin-users"><header><span>ACCOUNT</span><span>BALANCE</span><span>ROUNDS</span></header>{users.map(user=><div key={String(user.id)}><b>{String(user.display_name)}</b><span>${Number(user.balance).toLocaleString()}</span><span>{Number(user.rounds_played)}</span></div>)}</div></section></PageFrame>}
