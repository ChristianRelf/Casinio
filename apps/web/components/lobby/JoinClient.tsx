"use client";
import { useEffect, useState } from "react";import { useRouter } from "next/navigation";import { api, post } from "../../lib/client/api";import { Brand } from "../ui/Brand";
export function JoinClient({code}:{code:string}){const router=useRouter();const[invite,setInvite]=useState<{tableId:string;tableName:string;status:string}|null>(null);const[error,setError]=useState("");useEffect(()=>{api<{tableId:string;tableName:string;status:string}>(`invites/${code}`).then(setInvite).catch(reason=>setError(reason instanceof Error?reason.message:"Invite is invalid"))},[code]);async function join(){try{const result=await post<{tableId:string}>("tables/join",{code});router.push(`/table/${result.tableId}`)}catch(reason){setError(reason instanceof Error?reason.message:"Could not join")}}return <main className="join-screen"><Brand/><section><span className="overline">PRIVATE INVITE</span>{invite?<><h1>A seat at<br/><em>{invite.tableName}</em></h1><p>The table is {invite.status==="in_round"?"mid-round, so you’ll arrive as a spectator":"open and waiting"}. You can take a free seat between rounds.</p><button className="cream-button" onClick={join}>Join table</button></>:error?<><h1>This invitation<br/><em>has left the building.</em></h1><p>{error}</p><button className="outline-button" onClick={()=>router.push("/lobby")}>Return to lobby</button></>:<><h1>Checking the guest list.</h1><p>One restrained moment, please.</p></>}</section><small>Play money only. No cash value, purchases, or prizes.</small></main>}
